export type Restaurant = {
    id: string;
    clientId?: string | null; 
    name: string;
    description?: string | null; 
    city?: string | null; 
    createdAt?: string;
    updatedAt?: string;
  };